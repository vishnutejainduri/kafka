{
  "name": "stores-jdbc-source",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSourceConnector",
    "tasks.max": 1,
    "connection.url": "jdbc:oracle:thin:<DB_USERNAME>/<DB_PASSWORD>@//<DB_HOST_IP>:1521/<DB_SERVICE_NAME>",
    "schema.pattern": "MERCH",
    "table.poll.interval.ms": 3600000,
    "mode": "bulk",
    "query": "SELECT BUSINESS_UNIT_ID, SUB_TYPE, SITE_ID, DAYS_OPEN_PER_WEEK, \"NAME\", ADDRESS_1, ADDRESS_2, ADDRESS_3, ADDRESS_4, CITY, STATE_ID, ZIP_CODE, COUNTRY_ID, TELEPHONE, FAX, CAST(LATITUDE AS NUMBER(10,7)) AS LATITUDE, CAST(LONGITUDE AS NUMBER(10,7)) AS LONGITUDE FROM MERCH.SITES",
    "topic.prefix": "stores-connect-jdbc",
    "validate.non.null": "false",
    "numeric.mapping": "best_fit",
    "poll.interval.ms": 43200000,
    "offset.flush.timeout.ms": 60000
  }
}