packages:
  narvar:
    inputs:
      messagesMongoUri: $MESSAGES_MONGO_URI
      messagesMongoCertificateBase64: $MESSAGES_MONGO_CERTIFICATE_BASE64
      narvarUserName: $NARVAR_USER_NAME
      narvarPassword: $NARVAR_PASSWORD
