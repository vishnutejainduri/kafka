    rules:
      catalog-rule-ct:
        action: /${ORG}_${SPACE}/commercetools/consume-catalog-message-with-retry-ct
        trigger: catalog-ct-trigger
      barcode-rule-ct:
        action: /${ORG}_${SPACE}/commercetools/consume-barcode-message-with-retry-ct
        trigger: barcode-ct-trigger
