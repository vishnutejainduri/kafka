    triggers:
      catalog-ct-trigger:
        feed: ${MESSAGING_FEED_BINDING}/messageHubFeed
        inputs:
          isJSONData: true
          topic: styles-connect-jdbc-CATALOG
      price-ct-trigger:
        feed: ${MESSAGING_FEED_BINDING}/messageHubFeed
        inputs:
          isJSONData: true
          topic: prices-connect-jdbc
      sku-ct-trigger:
        feed: ${MESSAGING_FEED_BINDING}/messageHubFeed
        inputs:
          isJSONData: true
          topic: skus-connect-jdbc
      styles-basic-ct-trigger:
        feed: ${MESSAGING_FEED_BINDING}/messageHubFeed
        inputs:
          isJSONData: true
          topic: styles-basic-connect-jdbc
