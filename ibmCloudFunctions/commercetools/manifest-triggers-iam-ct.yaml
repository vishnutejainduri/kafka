    triggers:
      catalog-ct-trigger:
        feed: ${MESSAGING_FEED_BINDING}/messageHubFeed
        inputs:
          isJSONData: true
          topic: styles-connect-jdbc-CATALOG
      sku-ct-trigger:
        feed: ${MESSAGING_FEED_BINDING}/messageHubFeed
        inputs:
          isJSONData: true
          topic: skus-connect-jdbc
