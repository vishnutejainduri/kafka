    triggers:
      catalog-ct-trigger:
        feed: /${ORG}_${SPACE}/${FEED_BASE}/messageHubFeed
        inputs:
          isJSONData: true
          topic: styles-connect-jdbc-CATALOG
      barcode-ct-trigger:
        feed: /${ORG}_${SPACE}/${FEED_BASE}/messageHubFeed
        inputs:
          isJSONData: true
          topic: barcodes-connect-jdbc
      styles-basic-ct-trigger:
        feed: /${ORG}_${SPACE}/${FEED_BASE}/messageHubFeed
        inputs:
          isJSONData: true
          topic: styles-basic-connect-jdbc
      facets-ct-trigger:
        feed: /${ORG}_${SPACE}/${FEED_BASE}/messageHubFeed
        inputs:
          isJSONData: true
          topic: facets-connect-jdbc-STYLE_ITEM_CHARACTERISTICS_ECA
      sales-order-ct-trigger:
        feed: /${ORG}_${SPACE}/${FEED_BASE}/messageHubFeed
        inputs:
          isJSONData: true
          topic: sales-orders-connect-jdbc
      sales-order-details-ct-trigger:
        feed: /${ORG}_${SPACE}/${FEED_BASE}/messageHubFeed
        inputs:
          isJSONData: true
          topic: sales-order-details-connect-jdbc
